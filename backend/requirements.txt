# ---------------------------------------------------------------------
# CampusBite AI — Backend Requirements
# Python 3.11
# ---------------------------------------------------------------------

# --- Web framework -----------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Configuration & validation -----------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database ------------------------------------------------------------
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- Cache / message broker ----------------------------------------------
redis==5.2.1

# --- Auth (wired in a future milestone; pinned now for reproducibility) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- AI / ML (wired in future milestones) --------------------------------
google-generativeai==0.8.3
openai==1.58.1
sentence-transformers==3.3.1
faiss-cpu==1.9.0.post1

# --- Observability --------------------------------------------------------
python-json-logger==3.2.1

# --- Testing ---------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# --- Dev tooling -------------------------------------------------------------
ruff==0.8.4
mypy==1.14.0
pre-commit==4.0.1

# --- Auth (Patch 1: Authentication Foundation) ---------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
email-validator==2.2.0